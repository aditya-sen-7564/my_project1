it is the very good tools for push the our repository and it give the features like 
that will allow to us to see the path of the folder in the locally. and 
we can do mupliple tasks together without repeat the git command. repeatetively just like git bash.