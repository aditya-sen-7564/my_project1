it is tools used in version control system and databases to save and change permanentely

The git commit command captures a snapshot of your project's currently staged changes and permanently saves it to your local Git repository's history.